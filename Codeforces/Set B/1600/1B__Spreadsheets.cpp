#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str;
    cin >> str;
    int sz = str.size();
    int temp=0;
    for(int i=sz; i<=1; i--) {
        int p = (str[i-1] - 64)*26
    }
    int k = str[0] - 64;
    cout << temp << endl;
    return 0;
}
